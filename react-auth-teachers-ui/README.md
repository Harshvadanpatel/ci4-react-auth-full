# React Auth + Teachers UI

## Setup
1) `npm install`
2) Copy `.env.example` to `.env` and set `VITE_API_BASE` (defaults to http://localhost:8080/api)
3) `npm run dev`

## Pages
- /login, /register
- /users (protected)
- /teachers (protected, joined data)
